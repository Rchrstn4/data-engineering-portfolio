# Sales ETL

This project demonstrates a ETL pipeline using Python, pandas, and SQLite.

## Overview

The pipeline performs the following steps:

1. **Extract** raw sales data from a CSV file
2. **Transform** the data into daily product-level summaries
3. **Load** the transformed data into a SQLite database

## Tech Stack

- Python
- pandas
- SQLite

